# particle-pomdp

## Installation

Requires [JAX](https://github.com/jax-ml/jax) and [Flax](https://github.com/google/flax). Run
```bash
$ pip install -e ".[test]"
```
for an editable install with test dependencies.
